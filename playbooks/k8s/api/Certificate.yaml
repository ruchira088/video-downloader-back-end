apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-certificate
  namespace: "{{ k8s_namespace }}"
spec:
  secretName: api-tls-secret
  issuerRef:
    kind: ClusterIssuer
    name: lets-encrypt
  dnsNames:
    - "{{ hostname }}"
apiVersion: v1
kind: ConfigMap
metadata:
  name: forward-proxy-data-config-map
  namespace: "{{ k8s_namespace }}"
data:
{% for key, value in forward_proxy.config_values.items() %}
  "{{ key }}": "{{ value }}"
{% endfor %}

apiVersion: v1
kind: ConfigMap
metadata:
  name: forward-proxy-file-config-map
  namespace: "{{ k8s_namespace }}"
data:
{% for key, value in forward_proxy.file_config_values.items() %}
  "{{ key }}": |
{{ value | indent(4, first=true) }}
{% endfor %}



apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: forward-proxy-certificate
  namespace: "{{ k8s_namespace }}"
spec:
  secretName: forward-proxy-tls-secret
  issuerRef:
    kind: ClusterIssuer
    name: lets-encrypt
  dnsNames:
    - "{{ forward_proxy_hostname }}"
apiVersion: v1
kind: ConfigMap
metadata:
  name: batch-data-config-map
  namespace: "{{ k8s_namespace }}"
data:
{% for key, value in batch.config_values.items() %}
  "{{ key }}": "{{ value }}"
{% endfor %}

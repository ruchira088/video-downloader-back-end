apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: web-ingress
  namespace: video-downloader-api-{{ git_branch }}
spec:
  rules:
    - host: web.{{ git_branch }}.video.local.ruchij.com
      http:
        paths:
          - backend:
              serviceName: web-service
              servicePort: http4s-port
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secrets
  namespace: "{{ k8s_namespace }}"
type: Opaque
stringData:
{% for key, value in databases.postgres.secrets.items() %}
  "{{ key }}": "{{ value }}"
{% endfor %}

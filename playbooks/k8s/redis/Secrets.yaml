apiVersion: v1
kind: Secret
metadata:
  name: redis-secrets
  namespace: "{{ k8s_namespace }}"
type: Opaque
stringData:
{% for key, value in databases.redis.secrets.items() %}
  "{{ key }}": "{{ value }}"
{% endfor %}

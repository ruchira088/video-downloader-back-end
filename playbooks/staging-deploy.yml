---
- hosts: localhost
  connection: local

  tasks:
    - set_fact:
        k8s_namespace: video-downloader-back-end-staging
        hostname: api.staging.video.dev.ruchij.com
        env: development
        sentry_env: staging

    - name: Deploy databases
      import_tasks: tasks/databases-deploy.yml

    - name: Deploy to staging
      import_tasks: tasks/k8s-deploy.yml
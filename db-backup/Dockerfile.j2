FROM postgres

ENV GIT_BRANCH {{ git_branch }}
ENV GIT_COMMIT {{ git_commit }}
ENV BUILD_TIMESTAMP {{ build_timestamp }}

RUN apt update && \
    apt install awscli zip -y && \
    rm -rf /var/lib/apt/lists/*

ADD ./run-db-backup.sh /opt/

CMD [ "/opt/run-db-backup.sh" ]
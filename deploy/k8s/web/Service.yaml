apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: video-downloader-api-{{ git_branch }}
spec:
  selector:
    app: web
    branch: {{ git_branch }}
  ports:
    - name: http4s-port
      port: 8000